[config]
# Match saved profiles by EDID instead of by output name, so a screen is
# recognized even when it lands on a different port (e.g. HDMI-1 vs DP-1,
# or through a dock). Applies to every autorandr run, including the udev
# hotplug trigger. Same behaviour on every machine sharing these dotfiles.
match-edid=1
