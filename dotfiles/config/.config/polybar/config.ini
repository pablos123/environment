; ================================
; Polybar – Kanagawa inspired
; Simple color naming
; ================================

include-file = ~/.config/polybar/themes/rasmus_vague.ini

; ================================
; Bar
; ================================

[bar/left]
bottom = true

width = 20%
offset-x = 2.5%
height = 25
offset-y = 5

wm-restack = i3
override-redirect = true

radius = 10

background = ${colors.background}
foreground = ${colors.foreground}

border-size = 2
border-color = ${colors.blue}

padding-left = 1
padding-right = 1
module-margin = 1

font-0 = ZedMono Nerd Font:size=10;2

# Disable tray icons
tray-position = none
tray-padding = 1

enable-ipc = true
cursor-click = pointer
cursor-scroll = ns-resize

modules-left = workspaces
modules-center =
modules-right =

[bar/right]
bottom = true

width = 20%
offset-x = 77.5%
height = 25
offset-y = 5

wm-restack = i3
override-redirect = true

radius = 10

background = ${colors.background}
foreground = ${colors.foreground}

border-size = 2
border-color = ${colors.blue}

padding-left = 1
padding-right = 1
module-margin = 1

font-0 = ZedMono Nerd Font:size=10;2

# Disable tray icons
tray-position = none

enable-ipc = true
cursor-click = pointer
cursor-scroll = ns-resize

modules-left =
modules-center =
modules-right = pulseaudio networkwire network cpu memory date


; ================================
; Modules
; ================================

[module/workspaces]
type = internal/xworkspaces

label-active = %name%
label-active-foreground = ${colors.blue}
label-active-background = ${colors.gray}
label-active-padding = 2

label-occupied = %name%
label-occupied-foreground = ${colors.white}
label-occupied-padding = 2

label-empty = %name%
label-empty-foreground = ${colors.light-gray}
label-empty-padding = 2

label-urgent = %name%
label-urgent-foreground = ${colors.red}
label-urgent-padding = 2

; ================================
; Volume (PulseAudio / PipeWire)
; ================================

[module/pulseaudio]
type = internal/pulseaudio

use-ui-max = true
interval = 5

format-volume = <label-volume>
format-muted  = <label-muted>

label-volume =   %percentage%%
label-volume-foreground = ${colors.white}

label-muted =   muted
label-muted-foreground = ${colors.red}

click-right = sound_config

; ================================

[module/cpu]
type = internal/cpu
interval = 2

label = CPU %percentage%%
label-foreground = ${colors.green}

; ================================

[module/memory]
type = internal/memory
interval = 2

label = RAM %percentage_used%%
label-foreground = ${colors.green}

; ================================

[module/date]
type = internal/date
interval = 1

date = %d-%m-%Y
time = %H:%M
label = %date% %time%
label-foreground = ${colors.white}

[network-base]
type = internal/network
interval = 5
format-connected = <label-connected>
format-disconnected = <label-disconnected>

[module/wlan]
inherit = network-base
interface-type = wireless
label-connected =   %essid%
label-disconnected =   offline

[module/eth]
inherit = network-base
interface-type = wired
label-connected = wired
label-disconnected =


; ================================
; Settings
; ================================

[settings]
screenchange-reload = true
pseudo-transparency = false

