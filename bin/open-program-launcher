#!/usr/bin/env bash

# Program launcher

set -Eeuo pipefail

source "${HOME}/environment/lib/helpers.bash"

require_commands rofi

function main {
    exec rofi -show run -display-run ">"
}

main "$@"
