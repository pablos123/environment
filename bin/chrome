#!/usr/bin/env bash

# Chrome wrapper

set -Eeuo pipefail

source "${HOME}/environment/lib/helpers.bash"

require_commands google-chrome

function main {
    exec google-chrome \
        --enable-blink-features=MiddleClickAutoscroll \
        --enable-features=EnableNativeNotifications \
        "$@"
}

main "$@"
