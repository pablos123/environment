#!/usr/bin/env bash

set -Eeuo pipefail
shopt -s inherit_errexit

source "${HOME}/environment/lib/helpers.bash"

require_commands chrome

function main {
    exec chrome --incognito "$@"
}

main "$@"
