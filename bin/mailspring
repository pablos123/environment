#!/usr/bin/env bash

set -Eeuo pipefail
shopt -s inherit_errexit

source "${HOME}/environment/lib/helpers.bash"

require_commands /usr/bin/mailspring

function main {
    exec /usr/bin/mailspring \
        --password-store=gnome-libsecret \
        "$@"
}

main "$@"
