#!/usr/bin/env bash

# Open polybar

set -Eeuo pipefail

source "${HOME}/environment/lib/helpers.bash"

require_commands polybar polybar-msg

function main {
    quit_and_wait "polybar-msg cmd quit" polybar

    polybar left &
    polybar center &
    polybar tray &
    exec polybar right
}

main "$@"
