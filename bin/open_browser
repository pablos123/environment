#!/usr/bin/env bash
set -Eeuo pipefail

# --------------------------------------------------
# Open browser if sufficient memory available
# --------------------------------------------------

readonly MEMORY_THRESHOLD_KB=10000000

total_memory="$(free --total | awk '/^Mem:/ {print $2}')"

if (( total_memory > MEMORY_THRESHOLD_KB )); then
    exec chrome
fi
