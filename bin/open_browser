#!/usr/bin/env bash

function open_browser() {
    local total_memory

    total_memory="$(free --total | awk '/^Mem:/ {print $2}')"

    (( total_memory > 10000000 )) &&
        exec chrome
}

open_browser
