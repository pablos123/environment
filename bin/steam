#!/usr/bin/env bash

set -Eeuo pipefail
shopt -s inherit_errexit

source "${HOME}/environment/lib/helpers.bash"

require_commands flatpak

function main {
    exec flatpak run com.valvesoftware.Steam -system-composer "$@"
}

main "$@"
