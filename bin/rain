#!/bin/bash

quit=false
while getopts q opt; do
    case "$opt" in
    q) quit=true ;;
    *) echo "Invalid option" && exit 1 ;;
    esac
done

pkill -f myrain

if $quit; then pkill -f myrain; exit 0; fi

bash -c "exec -a myrain mpv $HOME/.audio/rain.mp3 &"
