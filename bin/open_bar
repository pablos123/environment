#!/usr/bin/env bash

function open_bar() {
    polybar-msg cmd quit
    while pgrep -u "${UID}" -x polybar >/dev/null; do sleep 0.3; done

    polybar left &
    polybar center &
    exec polybar right
}

open_bar
