#!/usr/bin/env bash

exec /usr/local/bin/st

# total_memory="$(free --total | awk '/^Mem:/ {print $2}')"
#
# if (( total_memory > 10000000 )); then
#     exec wezterm
# else
#     exec terminator
# fi
